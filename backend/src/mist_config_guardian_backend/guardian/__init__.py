"""Guardian: the audit-level impact investigation.

Pure domain code lives in this package. Persistence models are in ``models.guardian``, and API projections in
``schemas.guardian``. Nothing here reaches a database or transport on its own.
"""
